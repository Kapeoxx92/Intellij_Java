Zadanie (0.10 pkt)

Stwórz klasę Song, która modeluje pojedynczą piosenkę.

Klasa powinna posiadać trzy prywatne pola:

    title (String) — tytuł piosenki
    artist (String) — wykonawca
    durationSeconds (int) — czas trwania w sekundach

Klasa powinna posiadać:

    Konstruktor przyjmujący trzy argumenty: title, artist, durationSeconds
    Metody dostępowe: getTitle(), getArtist(), getDurationSeconds()
    Metodę getFormattedDuration() zwracającą czas w formacie MM:SS (minuty i sekundy, sekundy zawsze dwucyfrowe)
    Metodę toString() zwracającą: "[title]" by [artist] ([MM:SS])

Format wejścia: trzy linie — tytuł, artysta, czas w sekundach.

Format wyjścia: wynik metody toString().
``

Zadanie (0.10 pkt)

Stwórz klasę Athlete, która modeluje sportowca.

Klasa powinna posiadać trzy prywatne pola:

    name (String) — imię i nazwisko
    age (int) — wiek (musi być w przedziale [0, 120])
    personalBestSeconds (double) — rekord życiowy w sekundach (musi być > 0)

Klasa powinna posiadać:

    Konstruktor przyjmujący trzy argumenty (name, age, personalBestSeconds)
    Metody dostępowe: getName(), getAge(), getPersonalBestSeconds()
    Setter setAge(int age) — zmienia wiek tylko jeśli wartość mieści się w [0, 120]; w przeciwnym razie nic nie robi
    Setter setPersonalBestSeconds(double pb) — aktualizuje rekord tylko jeśli pb > 0; w przeciwnym razie nic nie robi
    Metodę toString() zwracającą: Athlete[name=[name], age=[age], pb=[personalBestSeconds]]

Format wejścia: pierwsza linia to komenda inicjalizująca INIT name age pb, kolejne linie to komendy:

    SET_AGE value — wywołaj setAge(value)
    SET_PB value — wywołaj setPersonalBestSeconds(value)
    PRINT — wypisz wynik toString()

Komenda                 Co robi                     Wywołanie Java

INIT Anna Nowak 25      tworzy obiekt Athlete       a = new Athlete("Anna Nowak", 25, 58.5)

58.5


SET_AGE 26              ustawia nowy wiek           a.setAge(26)


SET_PB 57.0             ustawia nowy rekord         a.setPersonalBestSeconds(57.0)


PRINT                   wypisuje opis               System.out.println(a.toString())




Zadanie (0.10 pkt)

Stwórz klasę Planet, która modeluje planetę układu słonecznego.

Klasa powinna posiadać trzy prywatne pola:

    name (String) — nazwa planety
    diameterKm (double) — średnica w kilometrach
    hasRings (boolean) — czy posiada pierścienie

Klasa powinna posiadać trzy przeciążone konstruktory, powiązane łańcuchowaniem przez this(...):

    Planet(String name, double diameterKm, boolean hasRings) — konstruktor główny (kanoniczy)
    Planet(String name, double diameterKm) — zakłada hasRings = false, deleguje do głównego
    Planet(String name) — zakłada diameterKm = 0.0 i hasRings = false, deleguje do konstruktora dwuargumentowego

Dodaj metodę toString() zwracającą: Planet[name=[name], diameter=[diameterKm], rings=[hasRings]]

Format wejścia: jedna linia z komendą:

    P3 [name] [diameterKm] [hasRings] — użyj konstruktora 3-argum.
    P2 [name] [diameterKm] — użyj konstruktora 2-argum.
    P1 [name] — użyj konstruktora 1-argum.

Komenda                     Co robi                         Wywołanie Java


P3 Saturn 116460.0 true     tworzy Planet (3 argumenty)     p = new Planet("Saturn", 116460.0, true)


P2 Earth 12742.0            tworzy Planet (2 argumenty)     p = new Planet("Earth", 12742.0)


P1 Nibiru                   tworzy Planet (1 argument)      p = new Planet("Nibiru")


Format wyjścia: wynik toString().



Zadanie (0.10 pkt)

Stwórz klasę Workout, która modeluje trening sportowy.

Klasa powinna posiadać prywatne pola:

    name (String) — nazwa treningu
    caloriesBurned (double) — spalone kalorie, domyślnie 0.0

Konstruktor: Workout(String name) — inicjalizuje nazwę, caloriesBurned = 0.0.

Dodaj trzy przeciążone wersje metody addCalories:

    addCalories(double amount) — dodaje podaną liczbę kalorii (jeśli amount > 0)
    addCalories(int amount) — konwertuje na double i dodaje (jeśli amount > 0)
    addCalories(double amount, int repetitions) — dodaje amount * repetitions (jeśli oba > 0)

Dodaj metodę getCaloriesBurned() i toString() zwracającą: Workout[name=[name], calories=[caloriesBurned]]

Format wejścia: pierwsza linia to nazwa treningu, kolejne linie to komendy:

    ADD_D [double] — wywołaj addCalories(double)
    ADD_I [int] — wywołaj addCalories(int)
    ADD_R [double] [int] — wywołaj addCalories(double, int)
    PRINT — wypisz toString()

Komenda                 Co robi                         Wywołanie Java


ADD_D 150.5             dodaje kalorie (double)         w.addCalories(150.5)


ADD_I 50                dodaje kalorie (int)            w.addCalories(50)


ADD_R 10.0 5            dodaje kalorie × powtórzenia    w.addCalories(10.0, 5)


PRINT                   wypisuje opis                   System.out.println(w.toString())



Zadanie (0.10 pkt)

Stwórz klasę Pet, która modeluje zwierzę domowe i śledzi liczbę wszystkich utworzonych zwierząt.

Klasa powinna posiadać:

    name (String, prywatne) — imię zwierzęcia
    species (String, prywatne) — gatunek (np. "cat", "dog")
    count (private static int) — licznik wszystkich utworzonych obiektów, startuje od 0

Konstruktor Pet(String name, String species) powinien inkrementować count przy każdym tworzeniu obiektu.

Dodaj:

    Gettery: getName(), getSpecies()
    Statyczną metodę getCount() zwracającą aktualną wartość count
    Metodę toString() zwracającą: Pet[name=[name], species=[species]]

Format wejścia: linie z komendami:

    NEW [name] [species] — utwórz obiekt Pet (zapamiętaj go jako ostatni)
    PRINT — wypisz toString() ostatnio utworzonego Pet
    COUNT — wypisz wynik Pet.getCount()

Komenda                 Co robi                         Wywołanie Java


NEW Mruczek cat         tworzy obiekt Pet               last = new Pet("Mruczek", "cat")


PRINT                   wypisuje opis ostatniego Pet    System.out.println(last.toString())


COUNT                   wypisuje licznik obiektów       System.out.println(Pet.getCount())



Zadanie (0.10 pkt)

Stwórz klasę narzędziową UnitConverter, która zawiera wyłącznie metody statyczne do konwersji jednostek.

Klasa nie powinna dawać się instancjonować — dodaj prywatny konstruktor bezargumentowy.

Zaimplementuj następujące statyczne metody:

    kmToMiles(double km) — konwertuje kilometry na mile: km * 0.621371
    milesToKm(double miles) — konwertuje mile na kilometry: miles / 0.621371
    kgToLbs(double kg) — konwertuje kilogramy na funty: kg * 2.20462
    lbsToKg(double lbs) — konwertuje funty na kilogramy: lbs / 2.20462
    isValidDistance(double km) — zwraca true jeśli km >= 0, inaczej false

Format wejścia: linie z komendami:

    KM_TO_MI [value] — wypisz wynik kmToMiles(value)
    MI_TO_KM [value] — wypisz wynik milesToKm(value)
    KG_TO_LBS [value] — wypisz wynik kgToLbs(value)
    LBS_TO_KG [value] — wypisz wynik lbsToKg(value)
    VALID [value] — wypisz wynik isValidDistance(value)

Komenda             Co robi                         Wywołanie Java


KM_TO_MI 10.0       konwertuje km na mile           System.out.println(UnitConverter.kmToMiles(10.0))


MI_TO_KM 1.0        konwertuje mile na km           System.out.println(UnitConverter.milesToKm(1.0))


KG_TO_LBS 70.0      konwertuje kg na funty          System.out.println(UnitConverter.kgToLbs(70.0))


LBS_TO_KG 100.0     konwertuje funty na kg          System.out.println(UnitConverter.lbsToKg(100.0))


VALID -1.0          sprawdza poprawność dystansu    System.out.println(UnitConverter.isValidDistan




Zadanie (0.10 pkt)

Stwórz klasę Prescription, która modeluje receptę medyczną.

Klasa powinna posiadać prywatne pola:

    medicationName (String) — nazwa leku, niezmienne po utworzeniu (final)
    dosageMg (double) — dawka w miligramach, niezmienne (final)
    refillsRemaining (int) — liczba pozostałych uzupełnień, mutowalny

Konstruktor Prescription(String medicationName, double dosageMg, int refillsRemaining) powinien:

    Rzucić IllegalArgumentException jeśli medicationName jest null lub pusty (po trim()), z komunikatem: Invalid medication name
    Rzucić IllegalArgumentException jeśli dosageMg <= 0, z komunikatem: Dosage must be positive
    Rzucić IllegalArgumentException jeśli refillsRemaining < 0, z komunikatem: Refills cannot be negative

Dodaj metodę useRefill() — zmniejsza refillsRemaining o 1, rzuca IllegalStateException z komunikatem No refills remaining jeśli już 0.

Dodaj gettery: getMedicationName(), getDosageMg(), getRefillsRemaining().

Metoda toString() zwraca: Prescription[med=[medicationName], dose=[dosageMg]mg, refills=[refillsRemaining]]

Format wejścia: komenda inicjalizująca, potem komendy:

    INIT [name] [dosageMg] [refills] — tworzy obiekt Prescription
    USE — wywołaj useRefill(), jeśli rzuci wyjątek wypisz jego message
    PRINT — wypisz toString()
    INIT_ERR [name] [dosageMg] [refills] — próba tworzenia obiektu; jeśli rzuci wyjątek, wypisz jego message

Komenda                 Co robi                 Wywołanie Java


INIT Ibuprofen 200.0    tworzy obiekt           p = new
3                       Prescription            Prescription("Ibuprofen", 200.0, 3)


USE                     zużywa jedno            p.useRefill()
                        uzupełnienie

PRINT                   wypisuje opis           System.out.println(p.toString())
                        recepty

INIT_ERR Lek -5.0 1     próba tworzenia         new Prescription("Lek", -5.0, 1) // rzuca wyjątek
                        (oczekiwany wyjątek)




Zadanie 0.1 pkt


Stwórz klasę GameCharacter, która modeluje postać w grze.

Klasa powinna posiadać:

    MAX_HEALTH — stała statyczna public static final int = 100
    nextId — prywatna statyczna int, startuje od 1
    id (private final int) — unikalny ID nadawany automatycznie
    name (private final String) — niezmienne
    health (private int) — punkty życia, domyślnie MAX_HEALTH
    level (private int) — poziom, domyślnie 1

Klasa powinna mieć dwa konstruktory powiązane przez this(...):

    GameCharacter(String name, int level) — konstruktor główny; waliduje: name nie może być null/pusty (komunikat: Name required), level musi być >= 1 (komunikat: Level must be >= 1); nadaje unikalny id i ustawia health = MAX_HEALTH
    GameCharacter(String name) — deleguje do głównego przez this(name, 1)

Dodaj metody:

    takeDamage(int dmg) — zmniejsza health o dmg (min. 0, max. MAX_HEALTH); ignoruje wartości <= 0
    heal(int amount) — zwiększa health o amount, ale nie przekracza MAX_HEALTH; ignoruje <= 0
    isAlive() — zwraca health > 0
    Gettery: getId(), getName(), getHealth(), getLevel()
    toString(): GameCharacter[id=[id], name=[name], health=[health], level=[level]]

Format wejścia: linie z komendami:

    NEW2 [name] [level] — utwórz przez konstruktor 2-arg, zapamiętaj jako bieżący
    NEW1 [name] — utwórz przez konstruktor 1-arg, zapamiętaj jako bieżący
    DMG [value] — wywołaj takeDamage
    HEAL [value] — wywołaj heal
    ALIVE — wypisz isAlive()
    PRINT — wypisz toString()
    ERR2 [name] [level] — próba tworzenia NEW2; jeśli wyjątek, wypisz message

Komenda             Co robi                 Wywołanie Java


NEW2 Hero 5         tworzy postać           current = new GameCharacter("Hero", 5)
                    (2 argumenty)

NEW1 Warrior        tworzy postać           current = new GameCharacter("Warrior")
                    (1 argument)

DMG 30              zadaje obrażenia        current.takeDamage(30)


HEAL 20             leczy postać            current.heal(20)


ALIVE               sprawdza czy żyje       System.out.println(current.isAlive())


PRINT               wypisuje opis postaci   System.out.println(current.toString())


ERR2 Villain 0      próba tworzenia         new GameCharacter("Villain", 0) // rzuca wyjątek
                    (oczekiwany wyjątek)




Zadanie 0.1 pkt


Stwórz klasę Aquarium, która modeluje akwarium.

Klasa powinna posiadać prywatne pola:

    name (String) — nazwa akwarium
    capacityLiters (double) — pojemność w litrach
    fishCount (int) — liczba ryb, startuje od 0

Konstruktor Aquarium(String name, double capacityLiters) powinien:

    Rzucić IllegalArgumentException z komunikatem Capacity must be positive jeśli capacityLiters <= 0

Dodaj metody:

    addFish(int n) — dodaje n ryb; rzuca IllegalArgumentException z komunikatem Count must be positive jeśli n <= 0; rzuca IllegalStateException z komunikatem Aquarium full jeśli po dodaniu przekroczyłoby się max pojemność (1 ryba = 10 litrów)
    removeFish(int n) — usuwa n ryb; rzuca IllegalArgumentException z komunikatem Count must be positive jeśli n <= 0; rzuca IllegalStateException z komunikatem Not enough fish jeśli nie ma tylu ryb
    transferTo(Aquarium other, int n) — przenosi n ryb z tego akwarium do other; używa wewnętrznie removeFish i addFish
    getMaxFish() — zwraca (int)(capacityLiters / 10)
    Gettery: getName(), getCapacityLiters(), getFishCount()
    toString(): Aquarium[name=[name], capacity=[capacityLiters]L, fish=[fishCount]/[maxFish]]

Format wejścia: komendy:

    AQUA [id] [name] [capacity] — utwórz akwarium pod danym id (0 lub 1)
    ADD [id] [n] — addFish; jeśli wyjątek, wypisz message
    REM [id] [n] — removeFish; jeśli wyjątek, wypisz message
    TRANS [fromId] [toId] [n] — transferTo; jeśli wyjątek, wypisz message
    PRINT [id] — wypisz toString() dla danego akwarium

Komenda             Co robi             Wywołanie Java


AQUA 0 MainTank     tworzy akwarium     aquariums[0] = new Aquarium("MainTank", 100.0)
100.0               pod id 0


ADD 0 5             dodaje 5 ryb do     aquariums[0].addFish(5)
                    akwarium 0

REM 0 2             usuwa 2 ryby z      aquariums[0].removeFish(2)
                    akwarium 0

TRANS 0 1 3         przenosi 3 ryby z   aquariums[0].transferTo(aquariums[1], 3)
                    0 do 1

PRINT 0             wypisuje opis       System.out.println(aquariums[0].toString())
                    akwarium 0





Zadanie
0.1 pkt
Opis⌄

Stwórz klasę Playlist, która modeluje playlistę muzyczną. Zadanie integruje wszystkie koncepty wykładu.

Klasa powinna posiadać:

    MAX_TRACKS — public static final int = 10
    totalCreated — private static int, startuje od 0, rośnie przy każdym new
    title (private final String) — tytuł playlisty
    owner (private final String) — właściciel
    trackCount (private int) — liczba dodanych utworów, startuje od 0
    totalDurationSeconds (private int) — łączny czas trwania, startuje od 0

Trzy konstruktory powiązane przez this(...):

    Playlist(String title, String owner) — konstruktor główny; waliduje: title i owner nie mogą być null/puste (komunikaty: Title required i Owner required); inkrementuje totalCreated
    Playlist(String title) — deleguje przez this(title, "Anonymous")
    Playlist() — deleguje przez this("Untitled")

Metody:

    addTrack(int durationSeconds) — dodaje utwór o podanym czasie; rzuca IllegalArgumentException("Duration must be positive") gdy <= 0; rzuca IllegalStateException("Playlist is full") gdy trackCount == MAX_TRACKS
    removeLastTrack(int durationSeconds) — usuwa ostatni utwór o podanym czasie trwania; rzuca IllegalStateException("Playlist is empty") gdy trackCount == 0
    mergeFrom(Playlist other) — próbuje dodać wszystkie utwory z other do tej playlisty (jako łączny czas podzielony równo); uproszczenie: dodaje other.trackCount razy średni czas (other.totalDurationSeconds / other.trackCount), zatrzymuje się gdy lista pełna; jeśli other jest pusta, nie robi nic
    getAverageDuration() — zwraca średni czas trwania jako double; zwraca 0.0 jeśli trackCount == 0
    Statyczna metoda: getTotalCreated()
    Gettery: getTitle(), getOwner(), getTrackCount(), getTotalDurationSeconds()
    toString(): Playlist[title=[title], owner=[owner], tracks=[trackCount]/10, duration=[totalDurationSeconds]s]

Format wejścia: komendy:

    NEW2 [title] [owner] — konstruktor 2-arg (zapisuje do kolejnego slotu: 0, potem 1)
    NEW1 [title] — konstruktor 1-arg
    NEW0 — konstruktor 0-arg
    ADD [id] [durationSeconds] — jeśli wyjątek, wypisz message
    REM [id] [durationSeconds] — jeśli wyjątek, wypisz message
    MERGE [fromId] [toId] — mergeFrom
    AVG [id] — wypisz getAverageDuration()
    TOTAL — wypisz getTotalCreated()
    PRINT [id] — wypisz toString()

Komenda             Co robi                 Wywołanie Java


NEW2 MyMix Alice    tworzy playlistę        pl[0] = new Playlist("MyMix", "Alice")
                    (2 argumenty)

NEW1 Chill          tworzy playlistę        pl[0] = new Playlist("Chill")
                    (1 argument)

NEW0                tworzy playlistę        pl[0] = new Playlist()
                    (0 argumentów)

ADD 0 180           dodaje utwór do         pl[0].addTrack(180)
                    playlisty 0

REM 0 180           usuwa ostatni utwór     pl[0].removeLastTrack(180)
                    z playlisty 0

MERGE 0 1           scala playlistę 0       pl[1].mergeFrom(pl[0])
                    do 1

AVG 0               wypisuje średni         System.out.println(pl[0].getAverageDuration())
                    czas utworu

TOTAL               wypisuje liczbę         System.out.println(Playlist.getTotalCreated())
                    utworzonych playlist

PRINT 0             wypisuje opis           System.out.println(pl[0].toString())
                    playlisty 0